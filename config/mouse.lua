-- Mouse configuration overrides

local config = {}

-- Set to "modules.mouse_management_spaces" to try the hs.spaces-backed module.
config.mouse = {
    management_module = "modules.mouse_management"
}

return config
